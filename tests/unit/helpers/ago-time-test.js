
import { agoTime } from 'hack/helpers/ago-time';
import { module, test } from 'qunit';

module('Unit | Helper | ago time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = agoTime([42]);
  assert.ok(result);
});

