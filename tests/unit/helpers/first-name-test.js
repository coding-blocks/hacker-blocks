
import { firstName } from 'hack/helpers/first-name';
import { module, test } from 'qunit';

module('Unit | Helper | first name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firstName([42]);
  assert.ok(result);
});

