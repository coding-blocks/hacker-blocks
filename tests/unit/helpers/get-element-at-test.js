
import { getElementAt } from 'hack/helpers/get-element-at';
import { module, test } from 'qunit';

module('Unit | Helper | get element at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getElementAt([42]);
  assert.ok(result);
});

